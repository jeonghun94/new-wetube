extends base.pug

block content
    div
        video#preview
        button#startBtn Start Recording
    form(method="POST", enctype="multipart/form-data").login__form
        input(type="file", accept="video/*", required, id="video", name="video")
        input(type="file", accept="image/*", required, id="thumb", name="thumb")
        input(placeholder="제목", required, type="text", name="title", maxlength=80)
        input(placeholder="설명", required, type="text", name="description", minlength=3)
        input(placeholder="해시태그 ,구분", required, type="text", name="hashtags")
        button 업로드

block scripts
    script(src="/static/js/recorder.js")        